<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuración de red de EVENTIVO (desarrollo, T-S1.11):
  el tráfico HTTP en texto plano SOLO se permite hacia los hosts locales de
  desarrollo (emulador Android -> 10.0.2.2, localhost). En producción la app
  se compila con --dart-define de una URL HTTPS y no aplica esta excepción.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>